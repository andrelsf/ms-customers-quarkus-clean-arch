quarkus:
  application:
    name: ms-customers
  shutdown:
    timeout: 5
  banner:
    enabled: false
  http:
    port: 8090
    root-path: /ms-customers/api
    access-log:
      enabled: true
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:mem:cybershopping
    username: cyber
    password: cyb3rs3cr3t
  hibernate-orm:
    database:
      generation: none
  liquibase:
    migrate-at-start: true
    change-log: "liquibase/changelog.yml"
  log:
    console:
      enable: true
      level: INFO
      format: "%d{HH:mm:ss} %-5p traceId=%X{traceId}, parentId=%X{parentId}, spanId=%X{spanId}, sampled=%X{sampled} [%c{2.}] (%t) %s%e%n"